<!-- mode: default — no calibration applied -->

# default

Baseline behavior — no mode-specific calibration.

## Headline behavior

Stock Claude Code presentation. Whatever the framework's normal voice is, that is what runs here.

## Notes

Default behavior, no calibration applied. This file exists so the resolver always has a fragment to return; the body is intentionally inert. If you are reading this fragment in a session, you should respond as you would without any mode active.
